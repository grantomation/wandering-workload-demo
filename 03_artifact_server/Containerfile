FROM node:22-alpine
WORKDIR /opt/artifact-server
COPY package.json ./
RUN npm install --omit=dev && \
    mkdir -p /var/www/files && chmod 775 /var/www/files
COPY server.js ./
COPY public/ ./public/
EXPOSE 8080
CMD ["node", "server.js"]
