apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: installation-dashboard-data
  namespace: workload-portability-build
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
